<%= page_header('Forbidden') %>

<%= error_message('Forbidden') do -%>
  <p>
    Oops, you clicked something we didn't expect you to click, and tried to do
    something you're not allowed to do. Sorry about that, the button shouldn't
    be there.
  </p>

<%- end -%>

<%= raw support_html %>
