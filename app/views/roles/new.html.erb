<%=
  breadcrumbs(
    { 'Administration' => root_path},
    { 'Roles' => roles_path },
    'New Role'
  )
%>

<%= page_header('New Role') %>

<%= render(partial: 'form', locals: { submit_label: 'Create' }) %>
