<%=
  breadcrumbs(
          { 'Administration' => dashboard_path },
          { 'API Accounts' => api_subjects_path },
    'New API Account'
  )
%>

<%= page_header('New API Account') %>

<%= render(partial: 'form', locals: { submit_label: 'Create' }) %>
